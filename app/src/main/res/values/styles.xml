<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/secondaryColor</item>
        <item name="android:windowBackground">@color/backgroundGeneralColor</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="text">
        <item name="android:fontFamily">@font/tillana</item>
    </style>

    <style name="textTitle" parent="text">
        <item name="android:textSize">@dimen/text_size_title</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="textGeneral" parent="text">
        <item name="android:textSize">@dimen/text_size_general_info</item>
    </style>

    <style name="textInfo" parent="text">
        <item name="android:textSize">@dimen/text_size_general_info</item>
    </style>

    <style name="textTitleUnderlined" parent="textTitle">
        <item name="android:text">@dimen/text_size_title</item>
    </style>

    <style name="button" parent="textTitle">
        <item name="android:background">@drawable/button_backgorund</item>
    </style>

</resources>
